#include "main.h"
/**
 * continuously - adds two numbers
 * @n1: first number
 * @n2: second number
 * Return: always success
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int i, j, k, l, m, n;
	for (i = 0; n1[i]; i++);
	for (j = 0; n2[j]; j++)
		return n2|n1+++
}
